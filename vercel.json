{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/files/(.*)",
      "dest": "/api/proxy?path=/files/$1"
    },
    {
      "src": "/api/(.*)",
      "dest": "/api/proxy?path=/$1"
    }
  ]
}
